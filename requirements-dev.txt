# Python Development Dependencies for Supabase Project
#
# This file lists Python tools used for development and CI/CD.
# These are not required for the application runtime (which uses Node.js/Deno),
# but are needed for code quality checks and linting.
#
# Installation:
#   pip install -r requirements-dev.txt
#
# Or using pipx for isolated environment:
#   pipx install sqlfluff

# SQL linting and formatting
# Used by: npm run lint:sql
# See: .sqlfluff for configuration
sqlfluff==3.0.7
